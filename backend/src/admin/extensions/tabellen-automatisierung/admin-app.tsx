/**
 * Admin App Extension for Tabellen-Automatisierung
 * Simplified version for Strapi 5 compatibility
 */

export default {
  config: {
    // Basic configuration without complex extensions for now
  },

  bootstrap(app: any) {
    // Simple bootstrap without translation registration
    console.log('Tabellen-Automatisierung Admin Extension loaded');
  }
};